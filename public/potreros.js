document.addEventListener('DOMContentLoaded', () => {
    console.log('Módulo de Gestión de Potreros cargado.');
    
    // Aquí podés agregar la lógica para cargar datos,
    // manejar eventos de la interfaz y actualizar
    // el estado de los potreros.
    
    initPotreros();
});

function initPotreros() {
    // Función de inicialización
    cargarDatosPotreros();
}

function cargarDatosPotreros() {
    // Simulación de carga de datos
    console.log('Cargando información de los potreros...');
}
