document.addEventListener('DOMContentLoaded', () => {
    console.log('Módulo de Control de Levante cargado.');
    
    // Aquí inicializamos la lógica específica de esta sección
    initLevante();
});

function initLevante() {
    cargarDatosLevante();
}

function cargarDatosLevante() {
    // Simulación de carga de datos para el levante
    console.log('Cargando información del ganado en levante...');
}
